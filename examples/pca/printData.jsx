#include "../../../basil.js/basil.js";#include "../../../JSON/json2.js"; // JSON.parse(myJson);function setup(){  b.doc('Design/Viskom Basel/Semester 5/TwitterMomentum/test.indd');}function draw() {  var pages = b.doc().pages;    var file = b.loadStrings('Design/Viskom Basel/Semester 5/TwitterMomentum/daten/1349543410enriched.json');  forEach( file, function(line,i){    if(i>=10) return false;    if(!b.page(i)) b.page(pages.add());    var data = JSON.parse(line);    b.textFont('Helvetica','Bold');    b.textSize(96);    var textFrame = b.text(data.interaction.content, 0,0,b.width,b.height);    b.typo(textFrame, 'pointSize', 50);    b.println(i);  });}b.go();